# ─────────────────────────────────────────────────────────────────
# AV-02 SSA — Dependências Python
# Compatível com apache/airflow:3.1.8
# ─────────────────────────────────────────────────────────────────

# Manipulação de dados (CSV, transformações)
pandas==2.2.3

# Conector PostgreSQL
psycopg2-binary==2.9.9

# Provider SQL comum — SQLExecuteQueryOperator
apache-airflow-providers-common-sql==1.14.2

# Provider PostgreSQL — conexão postgres_default
apache-airflow-providers-postgres==5.12.0

# Provider Standard — BashOperator, PythonOperator (já no core 3.x, explícito para garantia)
apache-airflow-providers-standard==0.0.3
