# Copie este arquivo para .env antes de rodar:
#   cp .env.example .env

# UID do usuário local (necessário para permissões dos volumes)
# Gerado automaticamente pelo setup.sh — não altere manualmente
AIRFLOW_UID=50000
